# 复制为 .env 后按需修改；.env 不要提交（已在 .gitignore 中）
# 数据库 / 应用密码（必填，至少 16 位，勿用示例值）
DB_PASSWORD=change-me-to-a-strong-password

# 初始账号密码（可选）：不设置则首次启动随机生成并打印在应用日志中一次
INIT_PASSWORD=

# 本地演示数据：首次启动写入 20 条样例预约（生产保持 false）
SEED_SAMPLE=false

# 经 HTTPS 反向代理对外提供服务时设为 true
COOKIE_SECURE=false
